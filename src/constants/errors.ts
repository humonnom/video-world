export const enum SignInError {
  NOT_FOUND = 'Not found',
  PASSWORD_INCORRECT = 'Password incorrect',
}

export const enum SignUpError {
  DUPLICATE_EMAIL = 'Duplicate email',
}

export const enum ProfileUpdateError {
  NOT_FOUND = 'Not found',
  PASSWORD_INCORRECT = 'Password incorrect',
}
